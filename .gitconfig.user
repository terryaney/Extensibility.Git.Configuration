[user]
	email = terry.aney@icloud.com
	name = Terry Aney

[core]
  # editor = notepad
  editor = "code -w --disable-extensions"
  hooksPath = ~/.gitHooks

# Set up default diff tool
[diff]
  # tool = bc3
  tool = diffmerge
  # tool = submlimemerge
  # tool = vscode

[merge]
  # tool = vscode

[safe]
	directory = *
