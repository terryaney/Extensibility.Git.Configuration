#!/bin/bash

# Read push refs from stdin once — stdin can only be consumed once so this must happen
# before any case blocks that previously had their own read loops.
CURRENT_BRANCH=""
CURRENT_COMMIT=""
PUSHED_MAIN_SHA=""
PUSHED_MAIN_REMOTE_SHA=""
while read local_ref local_sha remote_ref remote_sha
do
	if [[ $local_ref = refs/heads/main ]]; then
		CURRENT_BRANCH="main"
		PUSHED_MAIN_SHA="$local_sha"
		PUSHED_MAIN_REMOTE_SHA="$remote_sha"
		CURRENT_COMMIT=$(echo "$local_sha" | cut -c1-7)
	fi
done

# Helper: copy one blob from the pushed main commit into the documentation repository and stage it.
# Returns 0 only when the destination was actually updated.
# Usage: stage_doc_file <src_file> <dest_name> <dest_repo_path> <repo_root>
stage_doc_file() {
	local SRC_FILE="$1"
	local DEST_NAME="$2"
	local DEST_REPO_PATH="$3"
	local REPO_ROOT="$4"

	local tmp_file="$REPO_ROOT/.git/doc-sync-$CURRENT_COMMIT-$(basename "$SRC_FILE")"

	if ! git show "$PUSHED_MAIN_SHA:$SRC_FILE" > "$tmp_file" 2>/dev/null; then
		rm -f "$tmp_file"
		return 1
	fi

	if ! git -C "$DEST_REPO_PATH" diff --quiet -- "$DEST_NAME" 2>/dev/null || \
	   ! git -C "$DEST_REPO_PATH" diff --cached --quiet -- "$DEST_NAME" 2>/dev/null; then
		echo "Camelot Documentation Sync skipped $DEST_NAME — destination has unstaged/uncommitted changes."
		rm -f "$tmp_file"
		return 1
	fi

	if diff -q "$tmp_file" "$DEST_REPO_PATH/$DEST_NAME" > /dev/null 2>&1; then
		echo "Camelot Documentation Sync skipped $DEST_NAME — already up to date."
		rm -f "$tmp_file"
		return 1
	fi

	mkdir -p "$(dirname "$DEST_REPO_PATH/$DEST_NAME")"
	cp "$tmp_file" "$DEST_REPO_PATH/$DEST_NAME"
	git -C "$DEST_REPO_PATH" add "$DEST_NAME"
	rm -f "$tmp_file"
	return 0
}

# Helper: sync all files matching a pattern from the pushed main commit to a documentation repository as one commit.
# Source folder structure is flattened — every match lands directly in <dest_subfolder>.
# Usage: sync_doc_files <src_dir> <file_pattern> <dest_subfolder> <dest_repo_path> <dest_branch> <repo_label>
sync_doc_files() {
	local SRC_DIR="$1"
	local FILE_PATTERN="$2"
	local DEST_SUBFOLDER="$3"
	local DEST_REPO_PATH="$4"
	local DEST_BRANCH="$5"
	local REPO_LABEL="$6"

	[ "$CURRENT_BRANCH" = "main" ] || return

	local REPO_ROOT
	REPO_ROOT="$(git rev-parse --show-toplevel)"
	local STAGED=""

	local SAVED_GIT_DIR="${GIT_DIR:-}"
	local SAVED_GIT_WORK_TREE="${GIT_WORK_TREE:-}"
	unset GIT_DIR GIT_WORK_TREE

	while IFS= read -r src_file; do
		if stage_doc_file "$src_file" "$DEST_SUBFOLDER/$(basename "$src_file")" "$DEST_REPO_PATH" "$REPO_ROOT"; then
			STAGED="$STAGED $DEST_SUBFOLDER/$(basename "$src_file")"
		fi
	done < <(git ls-tree --name-only "$PUSHED_MAIN_SHA" "$SRC_DIR" 2>/dev/null | grep "$FILE_PATTERN")

	if [ -n "$STAGED" ]; then
		git -C "$DEST_REPO_PATH" commit -m "Automatically updated $DEST_SUBFOLDER docs from main:$REPO_LABEL, commit $CURRENT_COMMIT"
		git -C "$DEST_REPO_PATH" push origin "$DEST_BRANCH"
	fi

	[ -n "$SAVED_GIT_DIR" ] && export GIT_DIR="$SAVED_GIT_DIR"
	[ -n "$SAVED_GIT_WORK_TREE" ] && export GIT_WORK_TREE="$SAVED_GIT_WORK_TREE"
}

# Helper: sync files and/or folders (recursive) from the pushed main commit as one commit.
# If the pushed range touched any source path, the destination subfolder is replaced.
# Paths ending in / copy that folder's contents underneath <dest_subfolder>.
# Usage: sync_doc_paths <dest_subfolder> <dest_repo_path> <dest_branch> <repo_label> <src_path>...
sync_doc_paths() {
	local DEST_SUBFOLDER="$1"; shift
	local DEST_REPO_PATH="$1"; shift
	local DEST_BRANCH="$1"; shift
	local REPO_LABEL="$1"; shift

	[ "$CURRENT_BRANCH" = "main" ] || return

	local REPO_ROOT
	REPO_ROOT="$(git rev-parse --show-toplevel)"
	local STAGED=""

	local SAVED_GIT_DIR="${GIT_DIR:-}"
	local SAVED_GIT_WORK_TREE="${GIT_WORK_TREE:-}"
	unset GIT_DIR GIT_WORK_TREE

	local SOURCE_CHANGED=0
	if [ -n "$PUSHED_MAIN_REMOTE_SHA" ] && [[ ! "$PUSHED_MAIN_REMOTE_SHA" =~ ^0+$ ]]; then
		if git diff --name-only "$PUSHED_MAIN_REMOTE_SHA" "$PUSHED_MAIN_SHA" -- "$@" 2>/dev/null | grep -q .; then
			SOURCE_CHANGED=1
		fi
	else
		local probe_path
		for probe_path in "$@"; do
			if git ls-tree -r --name-only "$PUSHED_MAIN_SHA" -- "$probe_path" 2>/dev/null | grep -q .; then
				SOURCE_CHANGED=1
				break
			fi
		done
	fi

	if [ "$SOURCE_CHANGED" != "1" ]; then
		[ -n "$SAVED_GIT_DIR" ] && export GIT_DIR="$SAVED_GIT_DIR"
		[ -n "$SAVED_GIT_WORK_TREE" ] && export GIT_WORK_TREE="$SAVED_GIT_WORK_TREE"
		return
	fi

	if [ -z "$DEST_SUBFOLDER" ] || [ "$DEST_SUBFOLDER" = "." ]; then
		echo "Camelot Documentation Sync skipped empty destination subfolder."
		[ -n "$SAVED_GIT_DIR" ] && export GIT_DIR="$SAVED_GIT_DIR"
		[ -n "$SAVED_GIT_WORK_TREE" ] && export GIT_WORK_TREE="$SAVED_GIT_WORK_TREE"
		return
	fi

	if [ -n "$(git -C "$DEST_REPO_PATH" status --porcelain -- "$DEST_SUBFOLDER" 2>/dev/null)" ]; then
		echo "Camelot Documentation Sync skipped $DEST_SUBFOLDER — destination has unstaged/uncommitted changes."
		[ -n "$SAVED_GIT_DIR" ] && export GIT_DIR="$SAVED_GIT_DIR"
		[ -n "$SAVED_GIT_WORK_TREE" ] && export GIT_WORK_TREE="$SAVED_GIT_WORK_TREE"
		return
	fi

	rm -rf "$DEST_REPO_PATH/$DEST_SUBFOLDER"
	mkdir -p "$DEST_REPO_PATH/$DEST_SUBFOLDER"

	local src_path src_file src_prefix dest_name
	for src_path in "$@"; do
		src_prefix=""
		case "$src_path" in */) src_prefix="$src_path"; esac
		while IFS= read -r src_file; do
			dest_name="${src_file#"$src_prefix"}"
			mkdir -p "$(dirname "$DEST_REPO_PATH/$DEST_SUBFOLDER/$dest_name")"
			if git show "$PUSHED_MAIN_SHA:$src_file" > "$DEST_REPO_PATH/$DEST_SUBFOLDER/$dest_name" 2>/dev/null; then
				STAGED="$STAGED $DEST_SUBFOLDER/$dest_name"
			fi
		done < <(git ls-tree -r --name-only "$PUSHED_MAIN_SHA" -- "$src_path" 2>/dev/null)
	done

	git -C "$DEST_REPO_PATH" add -A "$DEST_SUBFOLDER"

	if ! git -C "$DEST_REPO_PATH" diff --cached --quiet -- "$DEST_SUBFOLDER"; then
		git -C "$DEST_REPO_PATH" commit -m "Automatically updated $DEST_SUBFOLDER docs from main:$REPO_LABEL, commit $CURRENT_COMMIT"
		git -C "$DEST_REPO_PATH" push origin "$DEST_BRANCH"
	else
		echo "Camelot Documentation Sync skipped $DEST_SUBFOLDER — already up to date."
	fi

	[ -n "$SAVED_GIT_DIR" ] && export GIT_DIR="$SAVED_GIT_DIR"
	[ -n "$SAVED_GIT_WORK_TREE" ] && export GIT_WORK_TREE="$SAVED_GIT_WORK_TREE"
}

# Documentation sync for all repos — skip if this is an internal mirror push
if [ "${DOC_SYNC_SKIP:-0}" != "1" ]; then

case "$2" in *tfs.acsgs.com/tfs/PDSI/HRS2/_git/HRS%20BTR%20-%20rcl.camelot.katapp|*github.com/terryaney/Camelot.RCL.KatApp.git)
	echo ""
	echo "Updating KatApp documentation..."
	echo ""
	sync_doc_files ".vscode/Documentation/" 'KatApp.*\.md$' "KatApp" "/c/BTR/Documentation/KAT" "main" "Camelot.RCL.KatApp"
esac

case "$2" in *tfs.acsgs.com/tfs/PDSI/HRS2/_git/HRS%20BTR%20-%20ess.camelot.nexgen|*github.com/terryaney/Camelot.ESS.Nexgen.git)
	echo ""
	echo "Updating Nexgen documentation..."
	echo ""
	sync_doc_files ".vscode/Documentation/" 'CalcEngines.*\.md$' "Nexgen" "/c/BTR/Documentation/KAT" "main" "Camelot.ESS.Nexgen"
esac

case "$2" in *tfs.acsgs.com/tfs/PDSI/HRS2/_git/HRS%20BTR%20-%20extensibility.camelot.excel.addin|*github.com/terryaney/Extensibility.Camelot.Excel.KAT.git)
	echo ""
	echo "Updating RBLe ExcelAddIn documentation..."
	echo ""
	sync_doc_files ".vscode/Documentation/" 'ExcelAddIn\.md$' "RBLe" "/c/BTR/Documentation/KAT" "main" "Extensibility.Camelot.Excel.AddIn"
esac

case "$2" in *tfs.acsgs.com/tfs/PDSI/HRS2/_git/HRS%20BTR%20-%20core.camelot.rble|*github.com/terryaney/Camelot.Core.RBLe.git)
	echo ""
	echo "Updating RBLe CalcEngines documentation..."
	echo ""
	sync_doc_files ".vscode/Documentation/" 'CalcEngines\.md$' "RBLe" "/c/BTR/Documentation/KAT" "main" "Camelot.Core.RBLe"
esac

case "$2" in *tfs.acsgs.com/tfs/PDSI/HRS2/_git/HRS%20BTR%20-%20extensibility.camelot.tfs.build.release|*github.com/terryaney/Extensibility.Camelot.TFS.Build.Release.git)
	echo ""
	echo "Updating TFS.Build.Release documentation..."
	echo ""
	sync_doc_paths "TFS.Build.Release" "/c/BTR/Documentation/KAT" "main" "Extensibility.Camelot.TFS.Build.Release" "Readme.md" "Assets"
esac

# KCC_DOC_REPO_PATH only exists so the sync can be rehearsed against a throwaway destination.
KCC_DOC_SYNC=0
case "$2" in *tfs.acsgs.com/tfs/PDSI/HRS2/_git/HRS%20BTR%20-%20extensibility.camelot.kat.command.center|*github.com/terryaney/Extensibility.Camelot.KAT.Command.Center.git)
	KCC_DOC_SYNC=1
esac

KCC_REPO_ROOT="$(git rev-parse --show-toplevel 2>/dev/null)"
KCC_REPO_ROOT="${KCC_REPO_ROOT//\\//}"
case "${KCC_REPO_ROOT,,}" in c:/btr/camelot/extensibility/kat.command.center|/c/btr/camelot/extensibility/kat.command.center)
	KCC_DOC_SYNC=1
esac

if [ "$KCC_DOC_SYNC" = "1" ]; then
	echo ""
	echo "Updating KAT.Command.Center documentation..."
	echo ""
	sync_doc_paths "KAT.Command.Center" "${KCC_DOC_REPO_PATH:-/c/BTR/Documentation/KAT}" "main" "Extensibility.Camelot.KAT.Command.Center" "docs/"
fi

fi

case "$2" in *Extensibility.Camelot.Excel.KAT.git.DISABLED)
	echo ""
	echo "Updating Excel-DNA documentation..."
	echo ""
	
	SOURCE_FILE="Readme.md" # replace with the file you want to copy
	DESTINATION_FILE_NAME="Readme.md" # replace with the new name you want for the file

	# Set the path to the destination repository and branch name
	DEST_REPO_PATH="/c/BTR/OpenSource/Extensibility.ExcelDNA.Sample"
	DEST_BRANCH="main"

	# Get the name of the current branch and commit hash
	while read local_ref local_sha remote_ref remote_sha
	do
		if [[ $local_ref = refs/heads/main ]]; then
			CURRENT_BRANCH="main"
			CURRENT_COMMIT=$(echo "$local_sha" | cut -c1-7)
		fi
	done

	# Check if the current branch is "main" and the file to copy exists
	if [ "$CURRENT_BRANCH" = "main" ] && [ -f "$SOURCE_FILE" ]; then
		# Copy the file to the destination repository with the new filename
		cp "$SOURCE_FILE" "$DEST_REPO_PATH/$DESTINATION_FILE_NAME"

		# Switch to the destination repository and checkout the destination branch
		cd "$DEST_REPO_PATH"
		# git checkout "$DEST_BRANCH"

		# Stage and commit the copied file with the new filename
		git add "$DESTINATION_FILE_NAME"
		git commit -m "Automatically updated file $DESTINATION_FILE_NAME from main:Extensibility.Camelot.Excel.KAT, commit $CURRENT_COMMIT"

		# Push the commit to the destination repository
		git push origin "$DEST_BRANCH"
	fi
esac

# GitHub mirrors pushed automatically after an origin push. Each name must be a configured
# remote whose URL carries its own account (https://<account>@github.com/...) so the
# credential manager hands each push the matching GitHub identity.
MIRROR_REMOTES="gh gh-conduent"

is_mirror_remote() {
	local candidate="$1"
	local remote
	for remote in $MIRROR_REMOTES; do
		[ "$candidate" = "$remote" ] && return 0
	done
	return 1
}

# Only mirror when the push targets the primary remote by name (not a URL, not a mirror itself)
if ! is_mirror_remote "$1" && [ "$1" != "conduent" ] && [[ "$2" != :* ]]; then
	MIRRORED=0
	for mirror in $MIRROR_REMOTES; do
		if git remote | grep -q "^${mirror}$"; then
			MIRRORED=1
			echo "Pushing to Github ($mirror)..."
			echo ""
			if DOC_SYNC_SKIP=1 git push "$mirror"; then
				echo ""
				echo "Github push complete ($mirror)."
			else
				echo ""
				echo "WARNING: push to $mirror failed — continuing with $1."
			fi
			echo ""
		fi
	done

	if [ "$MIRRORED" = "1" ]; then
		echo "Pushing to $1..."
		echo ""
	fi
fi

# Continue with the original push
exit 0